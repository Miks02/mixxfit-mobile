export type WorkoutListItem = {
    id: number,
    name: string,
    workoutDate: string,
    exerciseCount: number,
    setCount: number,
    hasCardio: boolean,
    hasWeights: boolean,
    hasBodyWeight: boolean
}
